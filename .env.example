# Copiar a .env y ajustar. Nunca commitear .env real (ver security/secrets, pendiente v0.3 S14).

TONY_ENV=development
LOG_LEVEL=INFO

# postgresql+psycopg://usuario:password@host:puerto/basededatos
DATABASE_URL=postgresql+psycopg://tony:tony@localhost:5432/tony
