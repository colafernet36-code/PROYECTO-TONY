# API interna

**Estado:** reservado, sin implementacion todavia.

**Responsabilidad prevista:** documentar los esquemas de `api/internal`, `api/websocket` y
`api/schemas` (peticion/respuesta, eventos en tiempo real hacia la UI).

**Cuando se implementa:** paso 13 del orden oficial de implementacion (v0.3 S19), junto con la
primera UI.
